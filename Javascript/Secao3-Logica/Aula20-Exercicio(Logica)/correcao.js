/* function max(x, y) {
   // if (x > y) return x;
    //return y;
    return x > y ? x : y;
} */

/* Aero function
const max2 = (x, y) => {
    return x > y ? x : y;
}
    */

const max2 = (x, y) => x > y ? x : y;
console.log(max2(10, 2));